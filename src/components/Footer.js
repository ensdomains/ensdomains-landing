import React from "react"
import styled from "@emotion/styled"

const FooterContainer = styled("footer")`
  background: green;
`

export default function Footer(props) {
  return <FooterContainer>Hero</FooterContainer>
}
